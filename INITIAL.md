# 当前候选能力点需求输入

> 本文件用于生成能力点 PRP，不等于实施授权。当前仍须等待用户说“下一步”。

## 能力点

M0-1：安全复制旧工程并建立迁移清单。

## 目标

把 `D:\CppLearn\chathub` 作为只读来源复制到 `D:\全栈聊天软件` 的最终工程结构中，保留一份可构建旧基线，为后续 Branch by Abstraction 渐进替换做准备。

## 用户价值

- 保护已完成的旧项目和学习证据；
- 避免直接重写导致长期不可运行；
- 建立可审查、可回退、可重复的迁移起点；
- 为简历项目留下真实的工程迁移证据。

## 当前上下文

- 产品合同：`docs/项目规划/首版产品方案.md`，v1.2，共 42 条需求。
- 详细设计：`docs/项目规划/详细设计与技术选型.md`，重点读取 D02、D63—D70、D175—D185、D208、D214 和 D231。
- 学习路线：`docs/项目规划/C++Qt学习路线-完善版.md`，当前为 M0／W0。
- 旧工程：`D:\CppLearn\chathub`，必须保持不动。
- 新工程：`D:\全栈聊天软件`，当前不是 Git 仓库，已有 `untitled` 原型和项目文档。

## 本步范围

- 只读盘点旧工程目录、源码、构建入口、依赖、测试和 Git 状态；
- 明确复制清单、排除清单、目标目录和名称冲突；
- 复制经确认的工程文件；
- 用路径、文件清单、哈希或 diff 证明旧工程未被修改；
- 记录复制后的基线构建入口和剩余环境依赖。

## 非目标

- 不改协议、架构、数据库、认证、UI 或业务行为；
- 不迁移数据库内容、真实账号、凭据、token、日志或证书私钥；
- 不删除或覆盖 `untitled`；
- 不初始化、暂存、提交或推送 Git，除非另有明确授权；
- 不开始 M0-2 或 M1。

## 排除项

- `.git/` 和旧仓库本地分支状态；
- `build/`、`cmake-build-*`、CMake cache、IDE cache；
- SQLite／MySQL 数据文件、导出、日志和临时文件；
- `.env`、密码、token、Credential／DPAPI blob；
- 根 CA／服务器私钥；
- 工程外和工程内的本地学习笔记；
- Node／npm cache 和其他可重建依赖目录。

## 完成条件

- [ ] 复制前清单、排除项和目标路径已由用户确认；
- [ ] 旧工程复制前后 Git 状态和关键文件哈希一致；
- [ ] 新目录不含秘密、数据库、构建产物和学习笔记；
- [ ] 复制结果有明确目录树和来源映射；
- [ ] 没有发生业务修改或 Git 写操作；
- [ ] 唯一学习笔记记录 M0-1 的状态和下一待办。

## 示例与参考边界

- `D:\CppLearn\chathub` 只能作为来源和实现蓝图，不能覆盖 v1.2 产品合同。
- `C:\Users\Administrator\Desktop\context-engineering-intro-main` 只作为上下文工程结构参考，其中 Python、Pydantic、Docker 和宽泛权限不适用于本项目。

## 需要在开始前确认

- 用户明确说“下一步”；
- M0-1 具体目标目录和 `untitled` 的保留方式；
- 哪些旧测试／文档属于复制范围；
- 是否只生成复制计划，还是执行实际复制。
