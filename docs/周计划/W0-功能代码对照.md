# W0 功能代码对照

> 更新时间：2026-09-01  
> 用途：把本周功能与真实代码／配置一一对应，做到代码与功能同步。每次能力点（或 Task）交付后更新本表。  
> 状态词汇：仅规划／已给出待落地／已实现未验证／自动验证通过／人工验收通过／已交付。

| 功能／能力点 | 关联决策 | 代码／配置落点 | 状态 | 验证证据 |
| --- | --- | --- | --- | --- |
| 根构建入口与 CTest 启用（M0-2） | D63—D70、D208 | `CMakeLists.txt`（`include(CTest)`、`add_subdirectory(shared)`） | 已交付 | `windows-mingw-debug` configure／build 通过（2026-09-01，用户执行＋本机复核） |
| 构建预设：Ninja＋MinGW＋vcpkg 三配置（M0-2） | D63—D70、D214 | `CMakePresets.json`（configure／build presets；构建目录隔离在 `out/build/<preset>`） | 已交付 | 同上 |
| 本机路径 User Preset（IDE 管理，不入库） | D208 | `CMakeUserPresets.json`（已被 `.gitignore` 忽略） | 已交付 | `git check-ignore` 确认忽略生效 |
| 依赖锁定 manifest（M0-2） | D214 | `vcpkg.json`（baseline `30ef65cad…`，triplet `x64-mingw-dynamic`，暂无第三方依赖） | 已交付 | 用户对齐 CLion 管理的 vcpkg HEAD |
| 公共合同接口 target（M0-2） | D02、D63—D70 | `shared/CMakeLists.txt`（`chathub::contracts` 接口 target，暂无源文件） | 已交付 | configure／build 通过 |
| Git 忽略边界（M0-1／M0-2） | D175—D185 | `.gitignore`（构建产物、IDE 目录、`vcpkg_installed/`、运行数据、秘密、路线文档） | 已交付 | `git status` 分类干净 |
| 目录职责与 target 边界规划（M0-1） | D02、D175—D185 | `PRPs/M0-1-空白工程根目录与构建边界规划.md`（文档级） | 已交付 | 文档级验收完成 |
| CTest smoke 测试与 test presets（M0-3） | D63—D70 | `tests/contracts_smoke.cpp`、`tests/CMakeLists.txt`、根 `CMakeLists.txt`（`BUILD_TESTING` 守卫）、`CMakePresets.json`（testPresets ×3） | 已交付 | CTest 1/1 通过、0 失败 0 跳过（用户回报 1.06s；本机复跑 0.40s 退出码 0，2026-09-01） |
| Git 提交范围收口（M0-4） | D175—D185 | Git 提交历史（`74533c2` ＋ 两次主题提交，推送 `origin/main`） | 已交付 | 提交与推送完成（2026-09-01，用户授权） |

## 同步规则

- 每次能力点（或 Task）交付后，由 AI 更新本表对应行；新增内容必须落到具体文件路径。
- 状态变化必须有当轮验证证据支撑；没有证据就保持原状态并注明原因。
- 本表只记录功能与代码的对应关系，不记录教学、讨论或计划性内容；那些内容属于 PRP 和学习笔记。
