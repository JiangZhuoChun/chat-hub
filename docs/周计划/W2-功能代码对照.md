# W2 功能代码对照

> 更新时间：2026-09-03
> 用途：把本周功能与真实代码／配置一一对应，做到代码与功能同步；每次交付后更新。
> 状态词汇：仅规划／已给出待落地／已实现未验证／自动验证通过／人工验收通过／已交付。

| 功能／能力点                              | 关联决策                   | 代码／配置落点                                                                                                                                                                                        | 状态     | 验证证据                                                                                    |
|-------------------------------------|------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------|-----------------------------------------------------------------------------------------|
| 锁定 OpenSSL 3.5 并引入 PKI target（M1-5） | D63、D208、D221          | `vcpkg.json`（`openssl`＋3.5.4 override）、`tools/pki/CMakeLists.txt`、根 `CMakeLists.txt`                                                                                                           | 自动验证通过 | OpenSSL 3.5.4（x64-mingw-dynamic）已装；CTest 5/5                                            |
| 开发 CA 签发与证书合同检查（M1-5）               | D06、D125、D126、D127     | `tools/pki/include/chathub/pki/dev_ca.hpp`、`tools/pki/src/dev_ca.cpp`、`tools/pki/src/dev_ca_main.cpp`                                                                                          | 自动验证通过 | `chathub.pki.dev_ca` 通过                                                                 |
| 私有 CA 单元测试（M1-5）                    | D125、D126              | `tests/pki_dev_ca_test.cpp`、`tests/CMakeLists.txt`                                                                                                                                             | 自动验证通过 | CTest `chathub.pki.dev_ca` 通过                                                           |
| 服务端 TLS 上下文（M1-6 Task 1）            | D62、D63、D126、D199、D221 | `vcpkg.json`、`chat-server/CMakeLists.txt`、`chat-server/include/chathub/server/transport/tls_context.hpp`、`chat-server/src/tls_context.cpp`、`tests/tls_context_test.cpp`、`tests/CMakeLists.txt` | 自动验证通过 | transport 重新编译成功；CTest 6/6                                                              |
| 客户端 TLS 配置（M1-6 Task 2）             | D05、D61—D63、D126       | `client-qt/CMakeLists.txt`、`client-qt/include/chathub/client/infrastructure/tls_client.hpp`、`client-qt/src/tls_client.cpp`                                                                     | 自动验证通过 | 客户端 target 已构建；CTest 6/6                                                                |
| 服务端 TLS 握手封装（M1-6 Task 3a）          | D05、D62、D73            | `chat-server/CMakeLists.txt`、`chat-server/include/chathub/server/transport/tls_handshake.hpp`、`chat-server/src/tls_handshake.cpp`                                                              | 自动验证通过 | transport 重新编译成功；CTest 6/6；真实握手行为并入 Task 3b                                             |
| 真实 TLS 集成（M1-6 Task 3b）             | D05、D61—D63、D73、D126   | `tests/tls_integration_test.cpp`、`tests/CMakeLists.txt`                                                                                                                                        | 自动验证通过 | `chathub.tls.integration` 通过；正确 CA＋IP 成功，错误 CA／IP 被拒绝，静默 peer 触发服务端 TLS 5 秒超时；CTest 7/7 |
| hello／capability（M1-7）              | D66、D68—D74、D197、D198、D218 | `PRPs/M1-7-hello与capability.md`；`shared/include/chathub/contracts/hello.hpp`；`chat-server/.../hello_codec.hpp`＋`.cpp`、`client-qt/.../hello_codec.hpp`＋`.cpp`；`tests/hello_test.cpp`、`tests/hello_codec_test.cpp`；`vcpkg.json`（boost-json）                                                                                                         | 自动验证通过 | `chathub.contracts.hello`、`chathub.hello.codec` 通过；双端往返一致＋wire 负例；CTest 9/9 |
| Session 超时／心跳（M1-8）                 | D55、D73                | （本周后续能力点）                                                                                                                                                                                      | 仅规划    | —                                                                                       |

## 同步规则

- 每次能力点（或 Task）交付后，由 AI 更新本表对应行；新增内容必须落到具体文件路径。
- 状态变化必须有当轮验证证据支撑；没有证据就保持原状态并注明原因。
- 本表只记录功能与代码的对应关系；问答类内容在 `docs/学习笔记/`（不入 Git）。
