# W1 功能代码对照

> 更新时间：2026-09-01  
> 用途：把本周功能与真实代码／配置一一对应，做到代码与功能同步；每次交付后更新。  
> 状态词汇：仅规划／已给出待落地／已实现未验证／自动验证通过／人工验收通过／已交付。

| 功能／能力点 | 关联决策 | 代码／配置落点 | 状态 | 验证证据 |
| --- | --- | --- | --- | --- |
| 服务端分层 targets：domain／application／infrastructure／transport（M1-1） | D199、D208、D221 | `chat-server/CMakeLists.txt`（4 个 INTERFACE target＋alias） | 已交付 | 干净目录 configure／build／CTest 1/1 退出码 0；负向验证触发 FATAL_ERROR（2026-09-01） |
| 客户端分层 targets：domain／application／infrastructure／presentation（M1-1） | D199、D208、D221 | `client-qt/CMakeLists.txt`（4 个 INTERFACE target＋alias） | 已交付 | 同上 |
| 根构建挂载＋架构断言（M1-1） | D221 | 根 `CMakeLists.txt`、`tests/CMakeLists.txt`（`chathub_check_direct_links` ×8） | 已交付 | 同上 |
| 强类型 ID／Outcome 头文件（M1-2） | D209、D211、D128、D44 | `shared/include/chathub/contracts/ids.hpp`、`outcome.hpp` | 自动验证通过 | Debug configure／build／CTest 2/2 通过、0 失败、0 跳过（2026-09-01） |
| 强类型单元测试（M1-2） | D209、D211 | `tests/contracts_types_test.cpp`、`tests/CMakeLists.txt` | 自动验证通过 | 同上 |
| ProtocolDescriptor 注册表（M1-3） | D218、D72、D74、D197 | `shared/include/chathub/contracts/protocol_descriptor.hpp` | 已交付 | 干净目录 CTest 3/3 退出码 0（首验 `hasState` 位掩码＋断言期望两处修正后，2026-09-01） |
| 协议注册表测试（M1-3） | D218 | `tests/contracts_protocol_test.cpp`、`tests/CMakeLists.txt` | 已交付 | 同上；保留 type nullptr 测试已补 |
| FrameDecoder 与编码器（M1-4） | D07—D09、D70 | `shared/include/chathub/contracts/frame.hpp` | 自动验证通过 | `windows-mingw-debug` 配置／构建成功；CTest 4/4 通过、0 失败、0 跳过（2026-09-01） |
| 帧层单元测试（M1-4） | D07—D09、D11 | `tests/frame_decoder_test.cpp`、`tests/CMakeLists.txt` | 自动验证通过 | 同上 |

## 同步规则

- 每次能力点（或 Task）交付后，由 AI 更新本表对应行；新增内容必须落到具体文件路径。
- 状态变化必须有当轮验证证据支撑；没有证据就保持原状态并注明原因。
- 本表只记录功能与代码的对应关系；问答类内容在 `docs/学习笔记/`（不入 Git）。
