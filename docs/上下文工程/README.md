# 上下文工程

规则唯一事实源是根目录 `AGENTS.md`。本目录只保留两份作用域资料：

- `上下文索引.md`：按任务类型加载最小充分上下文；
- `验证矩阵.md`：“检查”模式的分层验证条件（报告格式在 `.agent/commands/validate.md`）。

`LICENSE-来源模板.txt`：目录结构参考 context-engineering-intro-main（MIT）。
