# 全栈聊天软件 AI 协作规则

本文件是 `D:\全栈聊天软件` 的项目级上下文入口。所有输出、计划、注释和项目文档默认中文；代码标识符用清晰英文。只保留会影响 AI 决策和代码正确性的规则；解释、历史过程和重复内容不进入本文件。

## 1. 事实源优先级

冲突时按序处理：

1. 用户当前对话中的明确目标、范围和授权；
2. `TASK.md`、`INITIAL.md` 和当前能力点 `PRPs/*.md`；
3. `docs/项目规划/首版产品方案.md`；
4. `docs/项目规划/详细设计与技术选型.md`（D01—D231）；
5. `docs/项目规划/C++Qt学习路线-完善版.md`；
6. 当前源码、测试、构建输出和 Git diff；
7. 旧工程与外部参考。

代码只证明现状；模板、示例和外部文档不覆盖以上事实源。

## 2. 上下文按作用域加载

每次任务只加载：本文件、`TASK.md`、本步相关合同段落与 PRP、本步直接相关的源码／测试／构建配置／diff。加载地图见 `docs/上下文工程/上下文索引.md`。

- 全局规则只在本文件；模块规则只给相关模块；任务上下文只给当前任务。
- 禁止为“可能有用”加载整个仓库或全部 D01—D231。

## 3. 交互模式（只由用户指令切换）

- **常规（默认）**：AI 给出要实现和要修改的具体内容——文件、类／函数、代码、命令和预期结果；由用户落地、构建并观察。AI 不直接改写生产文件；用户明确要求“帮我实现／修改／修复”时才代写授权范围内的生产代码。
- **教学**：仅用户明确要求讲解时进入。平时不主动讲解、不布置复述、不设问答或解释门禁。
- **检查**：仅用户说“检查”时进入；按 `docs/上下文工程/验证矩阵.md` 分层验证并输出报告。
- **“下一步”**：直接进入下一功能点。新一周先产出 `docs/周计划/W?-需求与验收.md` 和 `W?-功能代码对照.md`，随后同轮交付该能力点的规划与实现内容，不等额外确认；范围异议随时叫停。
- 交付实现内容前必须显式列出假设；存在多种解释时全部列出，不静默选择；有更简单方案必须提出；阻塞性疑问先问再写。琐碎任务（注释、单行修复）可豁免流程。
- AI 已获授权在生产接口稳定后直接补充本步测试，不需确认；测试授权不含改变产品行为、公共协议、数据库结构或生产架构；为可测试性调整公共接口时，先列出改动由用户实现或另行授权。
- 掌握状态据实现／验证证据标注“未评估”或“待迁移验证”，仅在教学或检查时评估，不从沉默推定。

## 4. 代码规则

- 最小实现：只写被要求的功能。禁止投机抽象、未要求的可配置性、不可能场景的错误处理；200 行能写成 50 行就重写。
- 外科手术式修改：每行改动必须能追溯到当前请求。禁止顺手“改进”邻近代码、注释或格式；匹配现有风格；发现无关死代码只提及不删除；只清理自己的改动造成的孤儿 import／变量／函数。
- 禁止提前实现文件、语音、图片、群聊、Redis、微服务、WebSocket、gRPC、自动更新、备份或 E2EE。
- 新依赖必须说明必要性、MinGW／vcpkg 兼容性、许可证、版本锁定和移除成本。
- 技术栈固定：C++20（禁止编译器扩展）、Qt 6.11.1 Widgets、standalone Asio（禁止 Boost.Asio）、OpenSSL 3.5 LTS、Boost.JSON、MySQL 8.4.11＋MariaDB Connector/C、SQLite、libsodium、Windows 原生安全／服务 API。

## 5. 架构边界

- 客户端依赖方向 `ui → presentation → application → domain／ports ← adapters`；服务端 `transport → dispatcher → application → domain／ports ← infrastructure`。
- domain／application 禁止依赖 Qt、Asio、MySQL、OpenSSL、Win32、原始 JSON、SQL 或 QWidget。
- 对象组装只在 Composition Root 显式构造注入；禁止 Service Locator、全局 EventBus、隐藏业务单例、通用 CRUD Repository、巨型 ChatService。
- Session 只拥有 TLS、帧、超时、写队列和连接身份；业务只进类型化 Handler／Use Case，handler 禁止接触 socket／SQL。
- Wire DTO、Application Command／Query、Domain Object、Database Record、View Item 分开并显式映射；禁止一个 struct 穿透多层。
- 写事务提交成功后才返回成功并发布类型化 post-commit event；提交结果不明时禁止自动重放写操作。
- MySQL 访问只经 Repository／TransactionRunner；业务层禁止出现 `MYSQL*` 或 SQL。

## 6. 安全红线

- 所有外部输入验证存在性、类型、长度、编码、身份、权限。
- TLS 校验失败禁止忽略或降级明文；客户端只在应用内信任项目私有 CA，禁止修改 Windows 全局根证书库。
- 密码和重置码只存 Argon2id 哈希；session token 只存快速摘要；密码、重置码、token、密钥、消息正文禁止进日志。
- 客户端缓存正文用 XChaCha20-Poly1305，密钥由 DPAPI CurrentUser 保护；记住密码用 Credential Manager。
- 服务器为受信任局域网模型，管理员／SYSTEM／数据库高权限人员可读服务端正文；禁止宣传为 E2EE。

## 7. 测试与验收

- 每个 Task 先定可验证完成条件（真实命令＋预期退出码／观察），再实现；禁止“能跑就行”式目标。
- 生产能力点完成后，AI 集中补充与风险相称的测试；禁止只镜像实现的低价值测试。
- 至少覆盖适用场景：成功、边界、失败、重复、越权、断线、重连、持久化失败、状态交错。
- fake 只放 ports；真实 MySQL、TLS、Qt SQLite、双 profile、Windows 安装和人工 UI 验收不可被 mock 替代。
- 先构建活动 IDE 目录或干净指定目录，再运行 CTest；禁止用上次成功的二进制代替当前构建。
- SKIP、未运行、环境缺失不算通过；必须报告命令、退出码、通过／失败／跳过数。
- 失败先复现、缩小边界、找根因，再做最小修复；禁止削弱断言求绿。
- DPI、IME、托盘、视觉和完整用户体验保留人工验收。

## 8. 文档与 Git

- 状态只允许：仅规划／已给出待落地／已实现未验证／自动验证通过／人工验收通过／已交付。禁止把计划或旧证据写成当前完成。
- `TASK.md` 只保留一个进行中能力点（范围、非目标、状态、下一触发）；`INITIAL.md` 记录候选能力点；`PRPs/` 存能力点上下文包，必须引用真实文件和可运行命令。
- 每个新学习周先产出 `docs/周计划/W?-需求与验收.md` 和 `W?-功能代码对照.md`；每次交付后同步更新对照文档。
- 行为、协议、数据库、错误语义或验收变化时，同步正式项目文档和当前周对照文档。
- 本文件、产品方案、详细设计、学习路线为只读合同；仅用户明确要求变更时临时解锁，改后恢复只读。
- 工程外唯一学习笔记只记录用户主动提出的疑问。
- Git 写操作需明确授权；暂存明确文件，禁止 `git add .`，禁止覆盖用户或其他任务的改动。
- 数据库、缓存、日志、凭据、token、证书私钥、构建目录和本地学习笔记禁止进 Git。
- 旧工程 `D:\CppLearn\chathub` 只读参考，禁止修改或复制其代码、构建配置或运行依赖。

## 9. 完成报告

- 当前能力点与模式；本轮产出（常规＝实现与修改内容，检查＝结论与证据）；
- 若运行命令：命令、退出码、通过／失败／跳过数；
- `TASK.md`／PRP／周文档状态与范围审查（保留的无关改动）；
- 下一唯一待办（等待“下一步”“检查”或用户落地反馈）。
