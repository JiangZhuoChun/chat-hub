# 当前任务状态

> 更新时间：2026-09-01  
> 同一时间只允许一个进行中能力点。

## 当前状态

- 旧 ChatHub 的复制、迁移参考、副本和旧构建配置已取消，不属于新项目。
- 当前仓库仅保留产品／设计合同与协作骨架；没有生产代码、构建目录、依赖锁定或测试二进制。
- 当前没有进行中的实现能力点；等待用户说“下一步”后才启动新的 M0-1。

## 下一候选能力点

- 编号：M0-1
- 名称：空白工程根目录与构建边界规划
- 状态：仅候选，未授权实施

## 当前范围

- 保持 `D:\CppLearn\chathub` 不动且不复制；
- 从 `D:\全栈聊天软件` 的空白工程根目录开始；
- 后续先形成并确认新的 M0-1 PRP，再创建目录、CMake 或依赖配置。

## 非目标

- 恢复旧 ChatHub 源码、Node.js Auth Service、旧协议或旧构建配置；
- 修改产品需求、协议、数据库或 UI 行为；
- 安装或升级依赖；
- 启动 M1 协议／TLS 工作。

## 下一门禁

1. 用户说“下一步”；
2. AI 讲清空白工程边界、目录职责、CMake target 与验证策略；
3. 生成并由用户确认新的 M0-1 PRP；
4. 只实施该 PRP，不提前进入后续能力点。
